{
  "name": "@apps/admin",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "echo \"Replace with your framework's dev command (e.g., react-scripts start / next dev / expo start)\" && sleep 1",
    "build": "echo \"build step - replace with real build\""
  }
}